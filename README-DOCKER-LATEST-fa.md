نسخه داکري سايت موسسه علامه سخن
================================

اين بسته بر اساس نسخه زير ساخته شده است:

`APPROVED-BC7EE68-HERO-GRE-VISUAL-LOCK-BG-ADMIN-20260627-185500`

محتويات مهم:

- `Dockerfile`
- `docker-compose.yml`
- `compose.yaml`
- `server.js`
- `index.html`
- `package.json`
- `INSTALL-DOCKER-fa.md`
- `README-DOCKER-fa.md`

راه‌اندازي سريع:

1. فايل زيپ را روي سرور Extract کنيد.
2. داخل همان پوشه دستور زير را اجرا کنيد:

```bash
docker compose up -d --build
```

3. سايت روي پورتي که در `docker-compose.yml` تعريف شده بالا مي‌آيد.

نکته:

- اين نسخه همان آخرين نسخه اصلاح‌شده است و قابليت تغيير عکس زمينه هيـرو از پنل مدير را دارد.
- قالب و کاور آبي هيـرو با تغيير عکس زمينه حفظ مي‌شود.
