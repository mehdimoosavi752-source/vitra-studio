# راهنمای فعال‌سازی پیامک ملی‌پیامک

این نسخه ارسال پیامک را مستقیم از Backend انجام می‌دهد. کلید API در مرورگر نمایش داده نمی‌شود و نباید داخل GitHub قرار بگیرد.

## Render

در سرویس Render وارد بخش Environment شوید و این متغیرها را اضافه کنید:

```env
SMS_ENABLED=true
SMS_PROVIDER=melipayamak
SMS_USERNAME=نام کاربری ملی‌پیامک
SMS_API_KEY=رمز یا API Key ملی‌پیامک
SMS_SENDER=شماره فرستنده
SMS_ADMIN_NUMBERS=شماره‌های ادمین، هرکدام با کاما یا خط جدید
SMS_TEST_MOBILE=شماره تست اختیاری
SMS_MELIPAYAMAK_URL=https://rest.payamak-panel.com/api/SendSMS/SendSMS
```

بعد از ذخیره، سرویس را Redeploy یا Restart کنید.

## Docker / MySQL

در کنار `docker-compose.yml` فایل `.env` را ویرایش کنید و همین متغیرها را داخل آن بگذارید، سپس اجرا کنید:

```bash
docker compose up -d --build
```

## تست از پنل

در پنل مدیریت به بخش «پیامک و اعلان‌ها» بروید. وضعیت پیامک را فعال کنید، نوع پنل را `melipayamak` بگذارید، شماره فرستنده و شماره‌های مدیر را ثبت کنید، سپس با «ارسال پیامک تست» بررسی کنید.

اگر پیامک ارسال نشد، اول لاگ سرور را بررسی کنید. خطاهای رایج: اشتباه بودن شماره فرستنده، غیرفعال بودن خط خدماتی، اشتباه بودن API Key، یا محدودیت پنل پیامکی.
